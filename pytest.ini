[pytest]
markers =
    slow: slow tests needing real models (run explicitly with -m slow)
; Slow tests are excluded from the default dev loop; an explicit CLI -m overrides this.
addopts = -m "not slow"
